// import { signal } from '@angular/core';

export class PokedexNavigation {
  iv_misc_selection: 'notes' | 'trainers' | 'locations' = 'notes';
  iv_move_selection: 'learned' | 'tms' = 'learned';
  // card_view_selection = signal<'individual' | 'trainers'> ('individual');
}